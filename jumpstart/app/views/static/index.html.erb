<h1>jumpst<span><div class='title_font'>art</div></span></h1>

<hr>

<div class='row'>
  <div class='col-sm-4'>
    <ul>
      <% @images.each do |image| %>
      <li><%= link_to image_tag(image.image), user_path(image.user)  %></li>
      <% end %>
    </ul>
  </div>
</div>
